load std

MNT_DIR=/n/blackboxfs

test -d $MNT_DIR || mkdir -p $MNT_DIR
 

cd /host/blackboxfs/src
limbo -gw blackboxfs.b

ls
mount {blackboxfs} $MNT_DIR

mkdir -p /tmp/test
touch /tmp/test/a

bind /tmp/test $MNT_DIR/out


ns
cd $MNT_DIR
lc -l
